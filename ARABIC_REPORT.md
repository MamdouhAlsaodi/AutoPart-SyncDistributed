# تقرير تقني شامل - مشروع AutoPart-SyncDistributed

---

## 📋 ملخص المشروع

**AutoPart-SyncDistributed** هو تطبيق ويب متكامل لإدارة مخزون قطع غيار السيارات. يُمكّن المشروع الشركات من تتبع حركة القطع، ومراقبة مستويات المخزون، وإدارة الموردين والعملاء، و生成 التقارير Analytics.

### 🎯 الهدف التعليمي
هذا المشروع يُظهر كيفية بناء نظام إدارة مخزون كامل باستخدام:
- تتبع المخزون في الوقت الفعلي (Real-time)
- تسجيل حركات الدخول والخروج
- تنبيهات انخفاض المخزون
- إدارة الموردين والتصنيفات
- نظام مصادقة آمن
- رسوم بيانية وتحليلات

---

## 🏗️ بنية النظام

### هيكل المشروع
```
AutoPart-SyncDistributed/
├── client/                 # الواجهة (HTML + Tailwind + Vanilla JS)
│   ├── index.html         # الصفحة الرئيسية
│   ├── css/style.css     # التنسيقات
│   └── js/
│       ├── app.js        #逻辑 الواجهة
│       └── api.js        # API العميل
├── server/                # الخلفية (Node.js + Express)
│   ├── index.js          # نقطة الدخول
│   ├── seed.js           # بيانات أولية
│   └── src/
│       ├── app.js        # إعدادات Express
│       ├── config/db.js  # قاعدة البيانات في الذاكرة
│       ├── models/       # نماذج Mongoose
│       ├── routes/       # نقاط API
│       ├── controllers/  # منطق العمل
│       └── middleware/   # المصادقة والتحقق
```

### كيف يعمل النظام

```
┌─────────────┐     ┌──────────────┐     ┌─────────────────┐
│   المتصفح   │────▶│  Express.js  │────▶│  قاعدة البيانات │
│  (العميل)   │◀────│   السيرفر    │◀────│   (MongoDB)     │
└─────────────┘     └──────────────┘     └─────────────────┘
       │                    │                      ▲
       │              ┌──────┴──────┐               │
       │              │             │               │
   HTML/CSS        Auth +       Routes +        Mongoose
   Vanilla JS      JWT          Controllers      Models
```

---

## 🛠️ التقنيات المستخدمة

### الخلفية (Backend)
| التقنية | الاستخدام |
|---------|-----------|
| **Node.js** | بيئة تشغيل JavaScript |
| **Express.js** | إطار عمل الويب لـ REST API |
| **MongoDB** | قاعدة بيانات NoSQL (في الذاكرة) |
| **Mongoose** | نمذجة قاعدة البيانات |
| **JWT** | المصادقة بالتوكن |
| **bcryptjs** | تشفير كلمات المرور |
| **CORS** | حماية المصادر المشتركة |

### الواجهة (Frontend)
| التقنية | الاستخدام |
|---------|-----------|
| **HTML5** | هيكل الصفحة |
| **Tailwind CSS** | إطار التنسيق |
| **Vanilla JavaScript** | التفاعل من جانب العميل |
| **Chart.js** | الرسوم البيانية |
| **Font Awesome** | أيقونات |

---

## 🗄️ مخطط قاعدة البيانات

### النماذج (Models)

#### 1. قطعة الغيار (Part)
```javascript
{
  codigo: String,           // رقم القطعة (مثال: "ALT-008")
  nome: String,             // اسم القطعة
  descricao: String,       // الوصف
  preco_custo: Number,     // سعر التكلفة
  preco_venda: Number,     // سعر البيع
  estoque_atual: Number,   // المخزون الحالي
  estoque_minimo: Number,  // الحد الأدنى للمخزون
  categoria: String,       // التصنيف
  fornecedor: String,     // المورد
  ativo: Boolean,         // نشط/غير نشط
  criado_em: Date,        // تاريخ الإنشاء
  atualizado_em: Date    // آخر تحديث
}
```

#### 2. التصنيف (Category)
```javascript
{
  nome: String,           // اسم التصنيف
  descricao: String       // الوصف
}
```

#### 3. المورد (Supplier)
```javascript
{
  nome: String,           // اسم المورد
  cnpj: String,           // رقم التسجيل الضريبي
  telefone: String,      // رقم الهاتف
  email: String,         // البريد الإلكتروني
  ativo: Boolean         // نشط/غير نشط
}
```

#### 4. الحركة (Movement)
```javascript
{
  peca_id: String,       // رقم القطعة
  tipo: String,          // "entrada" (دخول) أو "saida" (خروج)
  quantidade: Number,   // الكمية
  usuario: String,       // المستخدم
  observacao: String,   // ملاحظات
  data: Date            // التاريخ
}
```

#### 5. المستخدم (User)
```javascript
{
  nome: String,           // اسم المستخدم
  email: String,          // البريد الإلكتروني
  senha: String,          // كلمة المرور المشفرة
  perfil: String          // الدور: "admin", "operador", "visualizador"
}
```

---

## 🔌 نقاط API

### المصادقة
| الطريقة | النقطة | الوصف |
|---------|--------|------|
| POST | `/api/auth/login` | تسجيل الدخول |
| GET | `/api/auth/me` | معلومات المستخدم الحالي |

### القطع
| الطريقة | النقطة | الوصف |
|---------|--------|------|
| GET | `/api/pecas` | عرض جميع القطع |
| GET | `/api/pecas/:id` | عرض قطعة معينة |
| POST | `/api/pecas` | إضافة قطعة جديدة |
| POST | `/api/pecas/entrada` | تسجيل دخول المخزون |
| POST | `/api/pecas/saida` | تسجيل خروج المخزون |
| GET | `/api/pecas/history` | سجل الحركات |

### التصنيفات والموردين
| الطريقة | النقطة | الوصف |
|---------|--------|------|
| GET | `/api/categorias` | عرض التصنيفات |
| GET | `/api/fornecedores` | عرض الموردين |

---

## 🔐 ميزات الأمان

1. **مصادقة JWT**
   - نظام توكن آمن لتسجيل الدخول
   - ينتهي صلاحية التوكن بعد 24 ساعة

2. **تشفير كلمات المرور**
   - استخدام bcryptjs مع ملح
   - لا يتم تخزين كلمات المرور بشكل نصي

3. **التحكم في الوصول حسب الأدوار (RBAC)**
   - **مدير (Admin)**: صلاحيات كاملة
   - **مشغل (Operator)**: تسجيل الحركات
   - **مشاهد (Viewer)**: قراءة فقط

4. **التحقق من المدخلات**
   - التحقق من جانب السيرفر
   - معالجة الأخطاء بشكل صحيح

5. **حماية CORS**
   - يسمح بالنطاقات المحددة

---

## 📊 البيانات الحالية

النظام يحتوي على بيانات تجريبية:

- **8 قطع غيار**: مانعة، أقماع فرامل، فلتر زيت، شمعات إشعال، إلخ
- **4 تصنيفات**: كهرباء، فرامل، محرك، تعليق
- **2 موردين**: AutoParts Brasil, MotorMax Ltda
- **2 مستخدمين**: 
  - مدير: admin@autopecas.com / admin123
  - مشغل: operador@autopecas.com / oper123

---

## 🚀 كيفية التشغيل

### المتطلبات
- Node.js مثبت (الإصدار 14 أو أحدث)

### التثبيت
```bash
# الانتقال إلى مجلد السيرفر
cd AutoPart-SyncDistributed/server

# تثبيت المتطلبات
npm install
```

### التشغيل
```bash
# تشغيل السيرفر
node index.js
```

أو مع بورت مخصص:
```bash
PORT=3001 node index.js
```

### الوصول
- **واجهة الويب**: http://localhost:3001
- **نقطة API**: http://localhost:3001/api/ping
- **تسجيل الدخول**: admin@autopecas.com / admin123

---

## 📱 شاشات النظام

1. **شاشة تسجيل الدخول** - مصادقة آمنة
2. **لوحة التحكم** - إحصائيات وتنبيهات انخفاض المخزون
3. **كتالogo القطع** - عرض وإدارة القطع
4. **الحركات** - تتبع حركات الدخول والخروج
5. **التقارير** - رسوم بيانية وتحليلات

---

## ⚡ الميزات الرئيسية المُحققَة

- ✅ تصميم RESTful API
- ✅ نظام مصادقة JWT
- ✅ قاعدة بيانات في الذاكرة للتطوير
- ✅ عمليات CRUD كاملة
- ✅ تتبع المخزون في الوقت الفعلي
- ✅ تنبيهات انخفاض المخزون
- ✅ تحكم في الوصول حسب الأدوار
- ✅ تصميم متجاوب
- ✅ تصور البيانات بالرسوم البيانية

---

## 📝 ملاحظات تقنية

1. **قاعدة البيانات في الذاكرة**: يستخدم المشروع `mongodb-memory-server` الذي يخلق MongoDB مؤقت في الذاكرة RAM. لا حاجة لتثبيت MongoDB خارجي.

2. servir الملفات الثابتة: Serve مباشرة Express ملفات الواجهة من مجلد `/client`.

3. **البيانات الأولية**: عند بدء التشغيل، يقوم النظام بتلقيم قاعدة البيانات ببيانات تجريبية.

4. **CORS**: مُفعل لجميع النطاقات أثناء التطوير.

---

## 🔗 الروابط

- **مستودع GitHub**: https://github.com/MamdouhAlsaodi/AutoPart-SyncDistributed
- **المطور**: ممدوح السدي
- **البريد الإلكتروني**: mamdouhalsaudi@gmail.com
- **LinkedIn**: https://www.linkedin.com/in/mamdouh-alsaudi-188693381/

---

*تم إعداد هذا التقرير للعرض التقديمي*
*المشروع: AutoPart-SyncDistributed الإصدار 1.0*
*المطور: ممدوح السدي*